package com.question.day02;

/**
 * 贩卖机只支持硬币支付，且收退都只支持10，50，100三种面额，一次购买只能出一瓶可乐，且投钱和找零都遵循优先使用大钱原则，
 * 需要购买可乐数量是m，其中手头拥有10，50，100的数量分别是a、b、c，可乐的价格是x（x是10的倍数），
 * 计算出需要投入硬币次数？
 *
 * @author wcl
 * @date 11:34 下午 2021/9/17
 */
public class Code02Cola {
    // 向上取整
    // giveRest
    // rest
    // zhang
    public static int putTimes(int m, int a, int b, int c, int x) {
        return 0;
    }
}
